1， 使用基本类型时， 尽可能的使用基本类型的包装类
原因： 在面对空值时， 由于基本类型具有默认值，导致通用mapper无法判断数据库数值是null还是默认值
	如 数据库中 id = 0   而 此时 bean 中 声明  int id;     那么此时的0是默认值还是数据库的值 通用mapper无法判断

2， 声明bean时 尽量将 @Id， @Column， @GeneratedValue 防止后续使用方法时导致的问题